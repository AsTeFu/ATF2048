/*
const buttons = new Map();
buttons.set("ArrowRight", {x: 1, y: 0});
buttons.set("ArrowLeft", {x: -1, y: 0});
buttons.set("ArrowUp", {x: 0, y: 1});
buttons.set("ArrowDown", {x: 0, y: -1});

document.addEventListener('keydown', (event) => {
    buttons.forEach((value, key) => {
        if (event.code === key) {
            console.log(value);
            const field = document.getElementById("field");
            for (let i = 0; i < field.childElementCount; i++) {
                field.children.item(i).style.left = '100px';
            }
        }
    });
});

function getTile(x, y) {
    document.getElementById(`tile-${x}-${y}`)
}

left = () => {
    const field = document.getElementById("field");
    for (let i = 0; i < field.childElementCount; i++) {

    }

    for (i = sizeGrid - 1; i >= 0; i++) {

    }
}; */