/*function createTile(availableTile) {
    let tile = {};
    tile.value = Math.random() > 0.9 ? 4 : 2;
    tile.x = availableTile.x;
    tile.y = availableTile.y;
    return tile;
}

function createTiles(amount) {
    let tiles = [];
    for (let i = 0; i < amount; i++) {
        tiles.push(createTile());
    }
    return tiles;
} */